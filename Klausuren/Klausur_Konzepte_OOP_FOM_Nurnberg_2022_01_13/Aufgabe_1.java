package Klausuren.Klausur_Konzepte_OOP_FOM_Nurnberg_2022_01_13;

public class Aufgabe_1 {
    
    /*
     * Aufgabe 1 (10 Punkte)
Kreuzen Sie die Aussagen an, die im Kontext von Klassen in Java korrekt sind.

 Eine abstrakte Klasse kann nicht Unterklasse einer nicht abstrakten Klasse
sein.
Falsch! (auch abstrakte Klassen erben bspw. von Object)

 Eine abstrakte Klasse kann als Unterklassen nur weitere abstrakte Klassen
haben.
Falsch! (wäre auch unsinnig)

 Fur abstrakte Klassen darf man keinen Konstruktor angeben, da diese Klassen ¨
nie instanziiert werden.
Falsch! Anders als Interfaces besitzen abstrakte Klassen sehr wohl einen Konstruktor.

 Eine abstrakte Klasse kann gleichzeitig mit den Modifikatoren final und
abstract gekennzeichnet sein.
Falsch! (wäre unsinnig und wird vom Compiler auch nicht akzeptiert)

 Der Modifier final schutzt Klassen vor Vererbung durch die Oberklasse. ¨
Falsch! (unsinnige Aussage)

 Der Modifier final schutzt Klassen vor Vererbung an die Unterklasse(n). ¨
Richtig!

 Wird eine Klasse als final deklariert, so sind damit automatisch nur alle
Methoden aber keine Variablen dieser Klassen vor Ver¨anderungen geschutzt. ¨
Richtig!

 In UML-Diagrammen werden abstrakte Klassen durch einen untrstrichenen
Klassennamen gekennzeichnet.
Falsch! Abstrakte Klassen werden kursiv geschrieben.

 Eine - typischerweise implizite - Typumwandlung von einem Untertyp zu einem
Obertyp heisst Upcast.
Richtig!

 Fur die Spezialisierung in der konkreten Klasse müssen unter allen Umständen
immer alle abstrakten Methoden der abstrakten Klasse uberschrieben werden.
Richtig! Wird eine Methode nicht überschrieben, ist die Spezialisierung selbst abstrakt.
*/

    
}
